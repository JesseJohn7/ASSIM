/*  document.getElementById('employmentForm').addEventListener('submit', function(event) {
    const form = event.target;
    const inputs = form.querySelectorAll('input[required]');

    let allFilled = true;
    inputs.forEach(input => {
        if (!input.value) {
            allFilled = false;
            input.style.borderColor = 'red';
        } else {
            input.style.borderColor = '#ccc';
        }
    });

    if (!allFilled) {
        event.preventDefault();
        alert('Please complete all fields.');
    }
});



if (firstName && lastName && employmentStatus && phoneNumber && email && password) {
    window.location.href = '../Sign Up 2/index.html';
} else {
    alert('Please fill all fields');
} 



function redirectToIndex(){
    window.location.href = "../Sign Up 3/index.html";
}

 */



/* document.getElementById('signupForm').addEventListener('submit', function(event) {
    event.preventDefault();

    const work = document.getElementById('work').value;
    const expertise = document.getElementById('expertise').value;
    const email = document.getElementById('email').value;
    const gov = document.getElementById('gov-id').value; */
   /*  const password = document.getElementById('password').value; */

 /*     if (fi && lastName && employmentStatus && phoneNumber && email && password) {
        window.location.href = '../Sign Up 3/index.html';
    } else {
        alert('Please fill all fields');
    } 
}); */




/* document.getElementById('signupForm').addEventListener('submit', function(event) {
    event.preventDefault();

    const firstName = document.getElementById('firstName').value;
    const lastName = document.getElementById('lastName').value;
    const employmentStatus = document.getElementById('employmentStatus').value;
    const phoneNumber = document.getElementById('phoneNumber').value;
    const email = document.getElementById('email').value;
    const password = document.getElementById('password').value;

    if (firstName && lastName && employmentStatus && phoneNumber && email && password) {
        window.location.href = 'nextPage.html';
    } else {
        alert('Please fill all fields');
    }
}); */